#include <iostream>

using namespace std;
int main()
{

    // Print all the numbers divisible by 3 && 5 from 100-500.
    /*
        int start=100, end=500, c=0;

        cout<<"All the numbers divisible by 3 && 5 from 100-500: ";
        int i = start;

        while (i<=end)
        {
            if ((i%3)==0 && (i%5)==0)
            {
                cout<<i<<", "; c++;
            }
            i++;
        }

        cout<<"\nTotal numbers = "<<c;
    */

    /*    int num, i;
        cout<<"To check if Prime or Composite Enter num:";
        cin>>num;

        cout<<"\nFactors of "<<num<<" = ";

        int counter =0;

        for ( i = 1; i <= num; i++)
        {
            if (i%2==0)
                continue;
            else
                cout<<i;
        }
    */

}